---
awg_dir: "/root/awg"
awg_manage_script: "{{ awg_dir }}/manage_amneziawg.sh"

# Permanent peers. Each item: { name: <peer>, purpose: <free text> }.
# Reconciled (created if missing), never recreated/removed by this role.
awg_clients: []
