global:
  scrape_interval: {{ prometheus_scrape_interval }}
  evaluation_interval: {{ prometheus_scrape_interval }}

# TODO(awg): add alert rule_files once AmneziaWG alerts are written.

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets:
          - prometheus:9090

  # TODO(awg): scrape AmneziaWG appliance exporters (node_exporter:9100,
  # amneziawg_exporter:9351, awg_peer_exporter via textfile collector).
  # Define an `awg_servers` inventory group and loop over it here, or have
  # the appliances push via Grafana Alloy remote_write instead of pull.
